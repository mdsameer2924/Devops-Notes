## Table of Content 
- [[File Management#What is File ?|What is File]]

### What is File ?
 file means storing data content in particular format digitally known as file where there are multiple types of like each store different types of data whether it's a video, audio, or text each comes with their extension 
 at the name of the file it's end with extension like `.txt`, `.mp4` , `mp3` , `.exe` , `.deb` etc
 **txt** file store text 
 **`mp4`** file stores either movie or audio
 **`mp3`** file stores only audio
each file different use case in [[Operating system]] some file use to store personal data and some file are Basically [[Application Software]] in Linux.  it's utilize Hardware and system Resources.

**for example** 
coder write code to make application and the source code written inside particular file 
where that file is not normal common file it's [[Programming Language|programming language's]] file  
such as `.py` , `.c` and `java` etc



and in linux here the term file hierarchy where each file has it own place to store for their valid reason 
in linux 