
| **command**                                                                                  | **use**                                              |
| -------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| `git init`                                                                                   | to intialize git                                     |
| `git config --global user.email <email>`<br>`git config -- global user.name <your username>` | to configuration username <br>and email , set author |
| `git add <filename`                                                                          | staging the file from untracked/<br>modified         |
| `git commit -m "message here"`                                                               | commit in git to add in git completely               |
| `git status`                                                                                 | to show the status of git stage                      |
I want to add something here which matter a lot. 